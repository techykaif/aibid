rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Ai-Bid uploads logos only through trusted server-side Firebase Admin code.
    // Browser/client Storage access stays disabled.
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
