import { ImageResponse } from "next/og";
import { db } from "@/lib/firebase-admin";
export const runtime="nodejs";
export const alt="ai-bid product leaderboard card"; export const size={width:1200,height:630}; export const contentType="image/png";
export default async function Image({params}:{params:Promise<{id:string}>}){const {id}=await params;const snap=await db.collection('products').doc(id).get();const p=snap.data();return new ImageResponse(<div style={{width:'100%',height:'100%',display:'flex',flexDirection:'column',justifyContent:'center',padding:'70px',background:'#080808',color:'white',fontFamily:'Arial'}}><div style={{fontSize:28,color:'#ff7a00',fontWeight:800}}>ai-bid.lol</div><div style={{fontSize:72,fontWeight:900,marginTop:28}}>{p?.name||'AI product'}</div><div style={{fontSize:30,color:'#aaa',marginTop:18}}>{p?.tagline||'Pay to rank.'}</div><div style={{display:'flex',gap:40,marginTop:60,fontSize:26}}><span>#{'—'} rank</span><span>${Number(p?.totalBidUSD||0).toFixed(0)} total bid</span></div></div>,size)}