import { db } from "@/lib/firebase-admin";
export const runtime="nodejs";
export async function GET(_:Request,{params}:{params:Promise<{productId:string}>}){const {productId}=await params;const snap=await db.collection('products').doc(productId).get();if(!snap.exists)return new Response('not found',{status:404});const p=snap.data()!;const total=Number(p.totalBidUSD||0);const products=await db.collection('products').where('status','==','live').orderBy('totalBidUSD','desc').limit(100).get();const rank=products.docs.findIndex(d=>d.id===productId)+1;const label=`ai-bid #${rank>0?rank:'?'}`;const text=`${p.name} · ${label}`;const width=Math.max(180,Math.min(500,text.length*8+36));const svg=`<svg xmlns="http://www.w3.org/2000/svg" width="${width}" height="32" role="img" aria-label="${text}"><rect width="${width}" height="32" rx="7" fill="#111"/><text x="12" y="21" fill="#ff7a00" font-family="Arial,sans-serif" font-size="12" font-weight="700">${escapeXml(p.name)}</text><text x="${width-12}" y="21" text-anchor="end" fill="#fff" font-family="Arial,sans-serif" font-size="12" font-weight="700">${escapeXml(label)} · $${total.toFixed(0)}</text></svg>`;return new Response(svg,{headers:{'Content-Type':'image/svg+xml','Cache-Control':'public, s-maxage=300, stale-while-revalidate=600'}})}
function escapeXml(value:unknown){return String(value).replace(/[<>&'"]/g,c=>({'<':'&lt;','>':'&gt;','&':'&amp;',"'":'&apos;','"':'&quot;'}[c]!))}